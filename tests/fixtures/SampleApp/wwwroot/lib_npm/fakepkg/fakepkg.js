/* Simulates an npm package copied into wwwroot by a build task (the nopCommerce
   lib_npm shape). Must be classified vendor by segment name, not substring. */
(function () {
    document.querySelector('#vendor-widget');
})();
