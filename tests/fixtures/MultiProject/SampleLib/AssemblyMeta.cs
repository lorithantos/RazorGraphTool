// Hand-written assembly attribute — the graph must show this one and NONE of
// the ~10 the SDK generates into obj\ per project.
[assembly: System.Reflection.AssemblyMetadata("fixture-marker", "sample-lib")]
